<span #if( $multiline )style="display:block;" #end>#displayMessage($box-for)</span>

#set($checkbox_id = 1)
#foreach( $box in $boxes )
    <label #if( $multiline )style="display:block;"><input name="$field-name" id="${field-name}.${checkbox_id}" class="jaffa-field"
    #if( $box.checked ) checked="checked" #end 
    type="checkbox"   #end value= #if( $box.value ) "$box.value" #else "$box.label" #end >#displayMessage($box.label)</label>
   
    #set($checkbox_id = $checkbox_id + 1)
#end