{
    "harvester": {
        "type": "file-system",
        "file-system": {
            "targets": [ {
                "baseDir": "${test.dir}"
            } ],
            "caching": "basic",
            "cacheId": "config-caching"
        }
    },
    "database-service": {
        "derbyHome" : "${test.cache.dir}"
    }
}
